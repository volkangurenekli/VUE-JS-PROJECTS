export const getCounter = (state) => {
  return state.counter;
};

export const getClick = (state) => {
  return state.counter + "  times clicked";
};

export const getValue = (state) => {
  return state.value;
};
